/*
    TASK:Possess
    LANG: CPP
    AUTHOR: KersaWC
    SCHOOL: REPS
*/
#include<stdio.h>
#include<math.h>
int main()
{
    double k,d,bk,s=15,x,y,xaxis,r,f,h;
    scanf("%lf %lf",&k,&d);
    if(k==40)
    {
       if((15+D)%120<60) printf("%.1lf",fmod((D+15),120);
       else if(fmod((15+d),120)>60) printf("%.1lf",120-fmod((15+d),120));
    }else
    if(k<40)
    {
       if(15+D*15/sqrt(15*15+(k-40)*(k-40))<60) printf("%.1lf",fmod(15+D*15/sqrt(15*15+(40-k)*(40-k)),120));
       else if(fmod(15+D*15/sqrt(15*15+(40-k)*(40-k)),120))
        }
    }else
    if(k>40)
    {
        bk=k-40;
        r=sqrt(pow(15,2)+pow(bk,2));
        f=(15*(d+r))/r;
        if(f<=60)
        {
            h=f*bk/15;
            printf("%.1lf %.1lf",f,k-h);
        }else
        if(f>60)
        {

        }
    }
    return 0;
}
