#include<stdio.h>
int main()
{
   char x;
   int l,r,j,c;
   scanf(" %c %d",&x,&l);
   if(r==2)
   {
      printf("%c %c",x,x);
   }else
   {
      j=r/2;
      for(r=1;r<=l;r++)
      {
         for()
      }

   }
}
